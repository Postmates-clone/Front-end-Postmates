import React from 'react';
import UserHeader from '../Items/UserHeader';

const OrderHistory = () => {
  return (
    <div>
      <UserHeader title="Orders" />
    </div>
  );
};

export default OrderHistory;
